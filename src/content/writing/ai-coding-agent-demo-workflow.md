---
title: "我如何使用 AI Coding Agent 构建小型 Demo"
description: "记录使用 Claude Code、Codex 等 AI Coding Agent 构建 Demo 的实际工作流和边界控制方法。"
date: 2026-05-26
tags: ["AI Tools", "Workflow", "Claude Code"]
status: published
category: "方法笔记"
topics:
  - ai-tools
  - personal-systems
  - writing
---

## 为什么我不把 AI Coding Agent 当成自动程序员

最开始用 AI Coding Agent 时，我试过把一个完整需求直接丢给它，让它从头到尾写完。

结果通常有两种：要么它写出一堆看起来能跑但结构混乱的代码，要么它在实现过程中自作主张改了我本来不想动的部分。

后来我意识到，AI Coding Agent 的优势不是"替代我写代码"，而是在我明确知道要做什么之后，帮我快速执行。判断、决策和验收这些环节，还是得由我来做。

所以我现在的做法是：把工作拆成几个阶段，让 Agent 只在合适的位置发挥作用。

## 我的基本分工

我目前用三个角色来划分工作：

**判断模型** - 负责理解需求、拆解问题、决定做什么和不做什么。这个角色通常是我自己，偶尔会用 Claude 或 GPT 帮我梳理思路，但最终判断由我来做。

**执行 Agent** - 负责写代码、改文件、跑命令。Claude Code、Codex、Antigravity 都可以承担这个角色。关键是给它明确的边界：只改这些文件，只做这件事，不要动其他东西。

**验收官** - 负责检查执行结果是否符合预期。包括本地 build、页面预览、功能验证、git status 检查。这个角色必须是我自己，不能交给 Agent。

## 一个 Demo 的最小工作流

我最近构建的几个 AI Demo（企业需求澄清 Agent、销售助理订单协同 Agent、相亲嘴替）都遵循类似的工作流：

### 需求澄清

先想清楚这个 Demo 要解决什么问题，边界在哪里。不需要写完整 PRD，但至少要回答：
- 这个工具给谁用？
- 输入是什么，输出是什么？
- 哪些功能必须有，哪些可以没有？

### 项目边界

确定技术选型和项目结构。对于小型 Demo，我通常选择：
- 前端：React 或 Vue，保持简单
- 后端：Cloudflare Workers 或 Pages Functions
- AI 接入：直接调用 API，不做复杂编排

### 本地实现

这一步交给执行 Agent。我会给它：
- 明确的文件范围（只改这几个文件）
- 明确的功能目标（实现这一个功能）
- 明确的约束（不要改 UI 样式，不要加新依赖）

### build 验收

每次 Agent 完成一轮执行后，我会：
1. 跑 `npm run build`，确认没有编译错误
2. 跑 `npm run preview`，在本地看页面效果
3. 检查 git status，确认没有意外修改

如果 build 失败，我会先看错误信息，判断是 Agent 的问题还是配置问题，再决定是让它修还是我自己修。

### 部署验收

本地验收通过后，才会部署到线上。部署后我会再检查一遍：
- 页面能正常打开
- 功能能正常使用
- 没有明显样式问题

### 回滚与交接

如果 Agent 的执行结果不满意，我会用 git 回滚到上一个 commit，然后重新给指令。

如果要把项目交接给另一个 Agent（比如从 Claude Code 换到 Codex），我会先写一份简短的交接文档，说明项目结构、当前状态和下一步要做什么。

## 我踩过的坑

### Agent 自作主张改 UI

有一次我让 Agent 实现一个 API 接口，它顺手把页面布局也改了。改完之后 build 能过，但页面样式全乱了。

后来我学乖了，每次给指令都会明确说"只改这几个文件，不要动 UI"。

### 一次性上下文太长

有一次我把整个项目的历史、需求、技术栈全塞进一个 prompt，结果 Agent 输出的内容前言不搭后语。

现在我会把上下文拆成多轮对话，每轮只聚焦一个具体问题。

### 没有先 commit 导致回滚困难

有一次 Agent 执行完之后，我发现结果不满意，想回滚，但之前没有 commit，git status 显示一堆改动，分不清哪些是 Agent 改的，哪些是我之前改的。

现在我会在 Agent 每次执行前先 commit 一个干净状态，这样回滚起来很清晰。

### 把 Mock 和真实 API 混淆

有一次我在本地用 Mock 数据测试通过了，部署后发现真实 API 返回的数据结构和 Mock 不一样。

现在我会尽早接入真实 API，哪怕只测一个接口，也比 Mock 靠谱。

## 当前我采用的原则

1. **每轮只做一个小目标** - 不要让 Agent 一次实现三个功能。
2. **先审计，再修改** - Agent 执行前，先让它读一遍相关文件，确认理解现状。
3. **先本地 build，再部署** - 不要在本地还没通过的情况下直接部署。
4. **不让执行 Agent 判断产品方向** - 执行 Agent 只负责写代码，不负责决定做什么。
5. **重要节点必须 commit** - 每完成一个可验收的功能，就 commit 一次。

## 后续还需要改进的地方

- 交接文档还不够结构化，有时候新的 Agent 接手时还是会问重复的问题
- 验收环节还不够自动化，大部分还是手动检查
- 还没有形成稳定的 prompt 模板，每次都要重新组织语言
- 对于复杂项目，分工边界还不够清晰

这些改进方向会随着后续项目实践逐步补充。
